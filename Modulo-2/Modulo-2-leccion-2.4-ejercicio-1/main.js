'use strict';

function multiply /*nombre de la funcion*/ (a, b) /*valores de la funcion*/ {
  return (
    a * b
  ); /*aqui es donde le digo la operacion que quiero hacer con ellos*/
}
/*aqui es donde le doy valores a (a y b) llamando a la funcion, en este caso multiply*/

console.log (multiply (3, 4));
console.log (multiply (5, 5));
console.log (multiply (6, 9));
